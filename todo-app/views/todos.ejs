<!-- todos.ejs -->
<form id="todoform" action="/todos" method="post" onsubmit="return validateForm()">
    <div class="flex gap-2 py-4">
        <div class="flex-auto">
            <input type="text" id="title" name="title" placeholder="What's next?" required class="text-sm p-2 border border-gray-300 text-gray-900 rounded leading-5" />
        </div>
        <div class="flex-auto">
            <input required type="date" name="dueDate" class="text-sm p-2 border border-gray-300 text-gray-900 rounded leading-5" />
        </div>
        <div class="flex-none">
            <button type="submit" name="submit" class="bg-green-600 text-white px-5 py-2 rounded leading-5 font-medium mr-2 mb-2">Add</button>
        </div>
    </div>
</form>

<script>
    function validateForm() {
    const titleInput = document.getElementById('title');
    if (titleInput.value.trim() === '') {
        alert('Please enter a title for your todo.');
        return false; 
    }

    if(dateInput = document.getElementById('dueDate').value.trim() === '') {
        alert('Please enter a due date for your todo.');
        return false; 
    }
    return true;
}

    async function setCompletionStatus(todoId) {
    const checkbox = document.getElementById(`todo-checkbox-${todoId}`);
    const completed = checkbox.checked;  // This will be true or false

    try {
        const response = await fetch(`/todos/${todoId}/update`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify({ completed }),
        });

        if (!response.ok) {
            throw new Error('Network response was not ok');
        }

        // Reload the page after successful update
        location.reload(); 
    } catch (error) {
        console.error('There was a problem with the fetch operation:', error);
        checkbox.checked = !completed;  // Revert checkbox state on error
    }
}

</script>

<div>
    <h5>Overdue</h5>
    (<span id="count-overdue"><%= todos.filter(todo => new Date(todo.dueDate) < new Date().setHours(0, 0, 0, 0) && !todo.completed).length %></span>)
    <table class="list-none">
        <thead>
            <tr>
                <!-- <th>Todo</th> -->
                <!-- <th>Actions</th> -->
            </tr>
        </thead>
        <tbody>
            <% for (var i = 0; i < todos.length; i++) { %>
                <% if (new Date(todos[i].dueDate) < new Date().setHours(0, 0, 0, 0) && !todos[i].completed) { %>
                    <tr class="Todo-Item">
                        <td>
                            <div class="flex items-center">
                                <input type="checkbox" 
                                       id="todo-checkbox-<%= todos[i].id %>" 
                                       class="w-4 h-4" 
                                       onclick="setCompletionStatus('<%= todos[i].id %>')" 
                                       <%= todos[i].completed ? 'checked' : '' %> />
                                <label for="todo-checkbox-<%= todos[i].id %>" class="ml-2 text-sm text-gray-600 cursor-pointer"><%= todos[i].title %></label>
                            </div>
                        </td>
                        <td>
                            <a href="#" onclick="deleteTodo('<%= todos[i].id %>')" class="trash-icon hidden ml-2">
                                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-8 px-2">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="m14.74 9-.346 9m-4.788 0L9.26 9m9.968-3.21c.342.052.682.107 1.022.166m-1.022-.165L18.16 19.673a2.25 2.25 0 0 1-2.244 2.077H8.084a2.25 2.25 0 0 1-2.244-2.077L4.772 5.79m14.456 0a48.108 48.108 0 0 0-3.478-.397m-12 .562c.34-.059.68-.114 1.022-.165m0 0a48.11 48.11 0 0 1 3.478-.397m7.5 0v-.916c0-1.18-.91-2.164-2.09-2.201a51.964 51.964 0 0 0-3.32 0c-1.18.037-2.09 1.022-2.09 2.201v.916m7.5 0a48.667 48.667 0 0 0-7.5 0" />
                                </svg>
                            </a>
                        </td>
                    </tr>
                <% } %>
            <% } %>
        </tbody>
    </table>
</div>






<div>
    <h5>Due Today</h5>
    (<span id="count-due-today"><%= todos.filter(todo => new Date(todo.dueDate).toDateString() === new Date().toDateString() && !todo.completed).length %></span>)
    <table class="list-none">
        <thead>
            <tr>
                <!-- <th>Todo</th> -->
                <!-- <th>Actions</th> -->
            </tr>
        </thead>
        <tbody>
            <% for (var i = 0; i < todos.length; i++) { %>
                <% if (new Date(todos[i].dueDate).toDateString() === new Date().toDateString() && !todos[i].completed) { %>
                    <tr class="Todo-Item">
                        <td>
                            <div class="flex items-center">
                                <input type="checkbox" 
                                       id="todo-checkbox-<%= todos[i].id %>" 
                                       class="w-4 h-4" 
                                       onclick="setCompletionStatus('<%= todos[i].id %>')" 
                                       <%= todos[i].completed ? 'checked' : '' %> />
                                <label for="todo-checkbox-<%= todos[i].id %>" class="ml-2 text-sm text-gray-600 cursor-pointer"><%= todos[i].title %></label>
                            </div>
                        </td>
                        <td>
                            <a href="#" onclick="deleteTodo('<%= todos[i].id %>')" class="trash-icon hidden ml-2">
                                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-8 px-2">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="m14.74 9-.346 9m-4.788 0L9.26 9m9.968-3.21c.342.052.682.107 1.022.166m-1.022-.165L18.16 19.673a2.25 2.25 0 0 1-2.244 2.077H8.084a2.25 2.25 0 0 1-2.244-2.077L4.772 5.79m14.456 0a48.108 48.108 0 0 0-3.478-.397m-12 .562c.34-.059.68-.114 1.022-.165m0 0a48.11 48.11 0 0 1 3.478-.397m7.5 0v-.916c0-1.18-.91-2.164-2.09-2.201a51.964 51.964 0 0 0-3.32 0c-1.18.037-2.09 1.022-2.09 2.201v.916m7.5 0a48.667 48.667 0 0 0-7.5 0" />
                                </svg>
                            </a>
                        </td>
                    </tr>
                <% } %>
            <% } %>
        </tbody>
    </table>
</div>


<div>
    <h5>Due Later</h5>
    (<span id="count-due-later"><%= todos.filter(todo => new Date(todo.dueDate) > new Date() && !todo.completed).length %></span>)
    <table class="list-none">
        <thead>
            <tr>
                <!-- <th>Todo</th> -->
                <!-- <th>Actions</th> -->
            </tr>
        </thead>
        <tbody>
            <% for (var i = 0; i < todos.length; i++) { %>
                <% if (new Date(todos[i].dueDate) > new Date() && !todos[i].completed) { %>
                    <tr class="Todo-Item">
                        <td>
                            <div class="flex items-center">
                                <input type="checkbox" 
                                       id="todo-checkbox-<%= todos[i].id %>" 
                                       class="w-4 h-4" 
                                       onclick="setCompletionStatus('<%= todos[i].id %>')" 
                                       <%= todos[i].completed ? 'checked' : '' %> />
                                <label for="todo-checkbox-<%= todos[i].id %>" class="ml-2 text-sm text-gray-600 cursor-pointer"><%= todos[i].title %></label>
                            </div>
                        </td>
                        <td>
                            <a href="#" onclick="deleteTodo('<%= todos[i].id %>')" class="trash-icon hidden ml-2">
                                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-8 px-2">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="m14.74 9-.346 9m-4.788 0L9.26 9m9.968-3.21c.342.052.682.107 1.022.166m-1.022-.165L18.16 19.673a2.25 2.25 0 0 1-2.244 2.077H8.084a2.25 2.25 0 0 1-2.244-2.077L4.772 5.79m14.456 0a48.108 48.108 0 0 0-3.478-.397m-12 .562c.34-.059.68-.114 1.022-.165m0 0a48.11 48.11 0 0 1 3.478-.397m7.5 0v-.916c0-1.18-.91-2.164-2.09-2.201a51.964 51.964 0 0 0-3.32 0c-1.18.037-2.09 1.022-2.09 2.201v.916m7.5 0a48.667 48.667 0 0 0-7.5 0" />
                                </svg>
                            </a>
                        </td>
                    </tr>
                <% } %>
            <% } %>
        </tbody>
    </table>
</div>


<div>
    <h5>Completed Items</h5>
    (<span id="count-completed"><%= todos.filter(todo => todo.completed).length %></span>)
    <table class="Todo-Item">
        <thead>
            <tr>
                <!-- <th>Todo</th> -->
                <!-- <th>Actions</th> -->
            </tr>
        </thead>
        <tbody>
            <% todos.forEach(todo => { %>
                <% if (todo.completed) { %>
                    <tr class="Todo-Item">
                        <td>
                            <div class="flex items-center">
                                <input type="checkbox" 
                                       id="todo-checkbox-<%= todo.id %>" 
                                       class="w-4 h-4" 
                                       onclick="setCompletionStatus('<%= todo.id %>')" 
                                       checked />
                                <label for="todo-checkbox-<%= todo.id %>" class="ml-2 text-sm text-gray-600 cursor-pointer"><%= todo.title %></label>
                            </div>
                        </td>
                        <td>
                            <a href="#" onclick="deleteTodo('<%= todo.id %>')" class="trash-icon hidden ml-2">
                                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-8 px-2">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="m14.74 9-.346 9m-4.788 0L9.26 9m9.968-3.21c.342.052.682.107 1.022.166m-1.022-.165L18.16 19.673a2.25 2.25 0 0 1-2.244 2.077H8.084a2.25 2.25 0 0 1-2.244-2.077L4.772 5.79m14.456 0a48.108 48.108 0 0 0-3.478-.397m-12 .562c.34-.059.68-.114 1.022-.165m0 0a48.11 48.11 0 0 1 3.478-.397m7.5 0v-.916c0-1.18-.91-2.164-2.09-2.201a51.964 51.964 0 0 0-3.32 0c-1.18.037-2.09 1.022-2.09 2.201v.916m7.5 0a48.667 48.667 0 0 0-7.5 0" />
                                </svg>
                            </a>
                        </td>
                    </tr>
                <% } %>
            <% }) %>
        </tbody>
    </table>
</div>


